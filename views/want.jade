extends layout

block content
    h2.text-center I'm looking for something

    .jumbotron.col-md-6.col-md-offset-3.col-sm-8.col-sm-offset-2
        form#update(role="form", method="post", action="/want")
            .form-group
                label(for="want") I want:
                select.form-control.input-lg(name="want", required)
                    option(value="null") Choose One
                    - for (trade in trades)
                        p test
                        option(value="#{trade}")= trades[trade]

            .row
                .col-sm-8.col-sm-offset-2
                    button.btn.btn-primary.btn-lg.btn-block(type="submit") Submit

